# Schemas

**Phase 1 working mirror.** These are byte-preserved copies of the immutable architecture schemas in `docs/architecture/schemas/`. They exist for tooling discoverability during Phase 1.

No new final schemas are invented in Phase 1. Repository-contract tests verify that each working mirror matches its immutable architecture copy byte-for-byte.
