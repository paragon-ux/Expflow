# Schemas

**Status:** working mirror of immutable architecture schemas.

These files are byte-preserved copies of the immutable architecture schemas in `docs/architecture/schemas/`. They exist for tooling discoverability and runtime/test validation during implementation.

The immutable copies under `docs/architecture/` remain the source authority. Repository-contract tests verify that each working mirror matches its immutable architecture copy byte-for-byte.
