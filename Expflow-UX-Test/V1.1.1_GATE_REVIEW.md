# Expflow v1.1.1 GUI Distribution Gate — Final Precision Review

**Review type:** Gate
**Base:** `e87759a7046fd2e9ad9b8c3b5ad17b3dc3656490` (current `main`)
**Final head:** `1708044934324934096568f2eb61c1efcd4a58b8`
**Review procedure:** `expflow-build-week-pr-review-precision`

---

## Verdict

**PASS** — all verified findings resolved. CI green across all platforms.

## Review target

- **Phase:** Release Gate v1.1.1 — GUI Distribution Ready
- **Scope:** 18 files, 4 review remediation commits on top of gate implementation
- **PR:** `fix/v1.1.1-gui-distribution`

## Commit history

```
1708044 fix: resolve lint errors in bridge.ts — type-safe entry digest
9f1288a style: format package-verify.ts
9ac1a8e fix: update protected config reference index for removed release.yml refs
40d7a72 fix: review remediation — all remaining findings
b2ea5d4 fix: review remediation — Origin port tolerance, token removal, restore conflict comparison
71e66a8 release: Expflow v1.1.1 GUI distribution gate
```

## Consolidated finding ledger

| ID | Priority | Finding | Status |
| -- | -------- | ------- | ------ |
| F1 | P0 Blocking | `validOrigins` exact string match rejected browser `Origin: http://127.0.0.1:PORT` | **FIXED** — `isOriginAllowed()` parses URL, compares scheme+hostname only |
| F2 | P2 | Request token printed to stdout, violating G1/G5 token secrecy | **FIXED** — `console.log` removed; token injected via HTML `<meta>` |
| F3 | P3 | Restore binding stored but never compared `conflicting_paths` during execution | **FIXED** — `executeRestore` recomputes and compares conflicts, refuses on mismatch |
| F4 | P3 | G3 candidate recompute-and-compare not implemented | **FIXED** — `changeDigest` computed from stable `(path, content_digest)` pairs; recomputed and compared on `executeSync` |
| Q1 | — | Dead `stored.consumed` field | **FIXED** — removed |
| Q2 | P3 | No TTL/expiry for stored syncPlans/restorePlans Maps | **FIXED** — evict oldest entry when Map size > 100 |
| N1 | Nit | `SHA256SUMS` omitted Python artifacts when included | **FIXED** — conditional inclusion when `SKIP_PYTHON=false` |
| N2 | Nit | No visible UI error when `pageToken` empty | **FIXED** — `statusPill` + `stateSummary` show visible error |
| — | CI | Config-reference reconciliation: stale release.yml ref in protected sidecar | **FIXED** — removed `.github/workflows/release.yml` entries from v1.0.1/v1.1.0 release notes sections |
| — | CI | Lint: `no-base-to-string`, `no-unnecessary-condition` in bridge.ts | **FIXED** — type-safe `entryDigestEntry` helper, removed unnecessary `??` |

## Gate outcome verification

| Gate | Requirement | Status |
| ---- | ----------- | ------ |
| G1 | POST-only mutations, Host/Origin/token enforcement | PASS |
| G2 | Blank/whitespace root rejected | PASS |
| G3 | Sync preview binding with change digest | PASS |
| G4 | Restore preview binding with conflict comparison | PASS |
| G5 | `expflow-gui` bin, apps/gui/ in npm package | PASS |
| G6 | Packed tarball verification | PASS |
| G7 | GUI smoke-flow document | PASS |
| G8 | V1-V4 documentation corrections | PASS |

## Validation

| Command | Result |
| ------- | ------ |
| `npm run validate` | 22/22 test files, 179/179 tests — all PASS |
| `python -m pytest` | 9/9 PASS |
| `npm run package:verify` | `expflow-gui` binary exists, installed 1.1.1 |
| `npm run check:config-references` | PASS — 0 unreconciled references |
| `npm run lint` | PASS — 0 errors |
| CI (macOS, Windows, Ubuntu × Node 20/22) | 5/5 checks PASS |
| External Package Verification | PASS |

## No remaining concerns

- Four-command CLI contract preserved (`expflow-gui` is separate bin)
- v1.1.0 provenance untouched
- Phases 8-9 remain deferred
- Python not republished (version 1.1.0 ≠ tag 1.1.1)
- No scope leakage
