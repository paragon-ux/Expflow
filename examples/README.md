# Examples

**Phase 1 working mirror.** These are byte-preserved copies of the supplied architecture examples in `docs/architecture/examples/`. Discoverability does not imply runtime conformance.

No new examples are invented in Phase 1. Repository-contract tests verify that each working mirror matches its immutable architecture copy byte-for-byte.
