# migration

**Gate D implementation.** This directory owns representative legacy-project migration evidence for the core repository.

Migration initializes Expflow in place, preserves user paths, records typed-folder inventory and limitations, and explicitly reports that no authority, semantic acceptance, workflow completion, or verification decisions were fabricated.

This directory does not implement adapter migration, external inspection, change cursors, or lost-response reconciliation.
