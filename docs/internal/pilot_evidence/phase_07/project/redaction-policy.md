# Redaction Policy

The pilot may record command names, relative paths, exit codes, elapsed time, and summarized stdout or stderr.

The pilot must not record secrets, credentials, personal data, hidden chain-of-thought traces, machine-absolute paths inside durable product evidence, or external private source material.

When evidence comes from the local machine, durable reports use repository-relative paths and commit identifiers.
