# Phase 1 Finding Inventory

**Phase:** 1 — Ordinary UX/UI Corrections
**Gate:** BW-A
**Baseline:** Expflow v1.0.1 @ `99ad975a2b3e1f4b6c67020b219db7ff6d64acf7` (branch `feat/build-week-governance`)
**Reproduction evidence:** `PHASE_01_BASELINE_TRANSCRIPT.txt`, `PHASE_01_BASELINE_RESTORE_SAFETY_TRANSCRIPT.txt` (same directory), plus source inspection of `src/cli/index.ts` and `src/operations/runtime.ts`.
**Evidence inputs note:** the retired `Expflow-Test/` meta-review reports (REVIEWS_INDEX, REVIEW_DISPOSITION_MATRIX, IMPLEMENTATION_FINDINGS) are not present locally. Per repository governance they are retired as active dependencies; every finding below was therefore reproduced directly against the current baseline source and CLI.

| ID  | Finding                                                                                                                                                                                                                       | Reproduction                                                                                   | Severity | Classification           | Decision                                                                         |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------- | ------------------------ | -------------------------------------------------------------------------------- |
| F1  | Terse human `status` (prints only `clean`/`drifted`/`uninitialized`)                                                                                                                                                          | Baseline transcript: `status` → `drifted`, `clean`                                             | High     | reproduced               | Implement actionable human status (prompt §8.1)                                  |
| F2  | Unhelpful uninitialized messaging (no next action)                                                                                                                                                                            | Baseline transcript: `status` → bare `uninitialized`, exit 0                                   | High     | reproduced               | Keep exit 0 (D1); human output explains state and recommends `expflow init`      |
| F3  | `sync --dry-run` reports a count, not a path plan                                                                                                                                                                             | Baseline transcript: `dry-run 2 node revision(s)`                                              | High     | reproduced               | Path-level deterministic dry-run plan (prompt §8.2)                              |
| F4  | Revision discovery gap: no ordinary way to find valid restore references                                                                                                                                                      | No history surface in CLI/status; restore requires `tree:`/`node:` refs                        | Critical | reproduced               | Revision discovery via additive `status` options (prompt §8.3)                   |
| F5  | No restore preview; `--dry-run` on restore is silently ignored and the restore EXECUTES                                                                                                                                       | Restore-safety transcript: `restore --dry-run tree:<id>` → `committed eft_8JA5...`             | Critical | reproduced               | Non-mutating restore preview sharing execution planning (prompt §8.4)            |
| F6  | Silent overwrite of conflicting unrecorded drift                                                                                                                                                                              | Restore-safety transcript: unsynced `version three` overwritten by restore, exit 0, no warning | Critical | reproduced               | Conflicting-drift refusal by default + explicit `--force` override (prompt §8.5) |
| F7  | Opaque identifier labels (`initialized efp_... at eft_...`, `committed eft_...`)                                                                                                                                              | Baseline transcript                                                                            | Medium   | reproduced               | Labeled identifiers in human output (prompt §8.6)                                |
| F8  | Provisional identity ambiguity: preview/pending `efn_*@1` revisions look committed; `provisional` absent from `src/`                                                                                                          | status --json `pending_changes`; source grep                                                   | High     | reproduced               | Explicit provisional labeling in human and machine output (R1, prompt §8.2/8.6)  |
| F9  | Remediation-poor errors: code/recoverable/recommended_action never shown; e.g. `Unsupported restore reference: HEAD`                                                                                                          | Baseline transcript; `src/cli/index.ts` catch-all                                              | High     | reproduced               | Remediation-first error formatting (prompt §8.7)                                 |
| F10 | Help gaps: no `--move`/`--new-node`/`--replace-node`/`--expected-head`, no restore reference syntax; per-command `--help` broken (`sync --help` RUNS a sync, `init --help` errors, `restore --help` treats flag as reference) | Baseline transcript                                                                            | Critical | reproduced               | Strict parsing + per-command help + full flag documentation (prompt §8.8)        |
| F11 | Unknown flags silently ignored: `sync --bogus-flag` commits drift with exit 0                                                                                                                                                 | Baseline transcript                                                                            | Critical | reproduced               | Unsupported option → usage failure exit 2 with remediation                       |
| F12 | `status --json` pending node revisions lack path mapping                                                                                                                                                                      | Baseline transcript JSON: `{kind: node_revision, revision}` only                               | Medium   | reproduced               | Additive `pending_change_details` with paths and kinds                           |
| F13 | Historical v1.0.0 post-restore node-revision collision                                                                                                                                                                        | Not reproduced on v1.0.1 (four prior reconstructions; none in this pass)                       | —        | not reproduced           | R2: keep historical; do not add to active taxonomy                               |
| F14 | `npm run check:protected-surfaces` required by validation order but missing from repository                                                                                                                                   | package.json/scripts and tools/ inspection                                                     | High     | reproduced (tooling gap) | Add repository-owned protected-surface checker (AGENTS.md §6)                    |

| F15 | Node restore recreated an unrecorded target-path deletion without explicit `--force` | Precision review reproduction against `5a84ef43fd2614303c030ce757d74bd22ea029b3`: delete tracked `a.txt`, run `restore node:<id>@1:a.txt`, restore commits and recreates the file | Critical | reproduced in precision review | Treat target-path deletion as conflicting drift; default restore refuses, explicit overwrite recreates |

Implementation evidence and test evidence columns are completed in the Phase 1 completion report.
