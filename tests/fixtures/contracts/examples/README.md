# Supplied Example Fixtures

The supplied architecture examples under `docs/architecture/examples/` are the authoritative example fixture source. `npm run schemas:examples-validate` validates them against the matching architecture schemas without copying their bytes into this directory.
